using UnityEngine;

namespace Cell_Folder
{
    public class Steam : Smoke
    {
        public Steam(int x, int y, GameObject gameObject, float speed, Grid grid, int code) : base(x, y, gameObject, speed, grid, code)
        {
        }
    }
}
